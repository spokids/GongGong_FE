// src/test.tsx
import { getSigungu, getDong, postProgram, getProgramDetail, getProgramReviewList, getProgramTop3, getProgramReviewed } from "@api/programAPI"

export default function Test() {
  // 2.1
  const clickGetSigungu = async() => {
    const response = await getSigungu("부산광역시");
    console.log(response);
  }

  // 2.2
  const clickGetDong = async() => {
    const response = await getDong("서울특별시", "강남구");
    console.log(response);
  }

  // 2.3
  const clickPostProgram = async() => {
    const response1 = await postProgram({province: "서울특별시", sigungu:"강남구", dong:"수서동"}, 1, 3)
    console.log(response1);
    const response2 = await postProgram({province: "서울특별시", sigungu:"강남구", dong:"수서동", age: 10, types:["수영", "헬스"]}, 1, 3)
    console.log(response2);
  }

  // 2.4
  const clickGetProgramDetail = async() => {
    const response = await getProgramDetail(2);
    console.log(response)
  }

  // 2.5 - 로그인 X

  // 2.6 프로그램 스크랩 취소 - 로그인 X
  
  // 2.6 프로그램 후기 리스트 조회
  const clickGetProgramReviewList = async() => {
    const response1 = await getProgramReviewList(1, 0, 10);
    console.log(response1);
    const response2 = await getProgramReviewList(1);
    console.log(response2);
  }

  // 2.7 - 로그인 X

  // 2.8
  const clickGetProgramTop3 = async() => {
    const response = await getProgramTop3();
    console.log(response);
  }

  // 2.9
  const clickGetProgramReviewed = async() => {
    const response1 = await getProgramReviewed();
    console.log(response1);
    const response2 = await getProgramReviewed(1, 3);
    console.log(response2);
  }

  // 2.10 - 로그인 X

  return (
    <div className="flex flex-col">
      <h1>test</h1>
      <button onClick={clickGetSigungu}>clickGetSigungu</button>
      <button onClick={clickGetDong}>clickGetDong</button>
      <button onClick={clickPostProgram}>clickPostProgram</button>
      <button onClick={clickGetProgramDetail}>clickGetProgramDetail</button>
      <button onClick={clickGetProgramReviewList}>clickGetProgramReviewList</button>
      <button onClick={clickGetProgramTop3}>clickGetProgramTop3</button>
      <button onClick={clickGetProgramReviewed}>clickGetProgramReviewed</button>
    </div>
  )
}